<div>
    <p>
        The team captains should add each other as friends in {{ $gameName }}.
        {{ $firstParticipantTitle }} captain  (<span style="color: #fb0025">{{ $firstParticipantCaptainUsername }}</span>) creates an in-game custom lobby according to the settings provided below:
    </p>
    <br />
    <p>
        {{ $firstParticipantTitle }} captain  (<span style="color: #fb0025">{{ $firstParticipantCaptainUsername }}</span>) needs to invite {{ $secondParticipantTitle }} captain  (<span style="color: #fb0025">{{ $secondParticipantCaptainUsername }}</span>).
    </p>
</div>
